{
    
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Open AWS D17.1 Calculator",
            "file": "${workspaceFolder}/Index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
